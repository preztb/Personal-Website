import React, { useEffect, useState, useRef } from 'react'

const urls = [
    
]

const SpinnerLoader = () => {

    useEffect(() => {
        setTimeout(() => {
            
        })
    })
  return (
    <div>
      
    </div>
  )
}

export default SpinnerLoader
